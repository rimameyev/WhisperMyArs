<Window x:Class="WhisperMyArs.UI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Focusable="False"
        IsHitTestVisible="False">

    <Border x:Name="Pill"
            CornerRadius="14"
            Background="#E61B1B1F"
            BorderBrush="#332E2E36"
            BorderThickness="1"
            Padding="18,10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="18" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <!-- Listening: animated bars -->
            <StackPanel x:Name="BarsPanel"
                        Orientation="Horizontal"
                        Height="34"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"/>

            <!-- Transcribing: pulsing label -->
            <TextBlock x:Name="StatusText"
                       Text="Transcribing…"
                       Foreground="#FFEDEDF2"
                       FontFamily="Segoe UI"
                       FontSize="15"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Visibility="Collapsed"/>
        </Grid>
    </Border>
</Window>
